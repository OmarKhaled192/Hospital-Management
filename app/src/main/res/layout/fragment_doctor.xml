<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@color/bgHomePatient"
    tools:context=".fragments.hospital.DoctorFragment">

    <TextView
        android:id="@+id/text_home"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="DoctorAdded"
        android:textAlignment="center"
        android:background="@drawable/drugs_bar"
        android:layout_margin="@dimen/_20sdp"
        android:padding="@dimen/_5sdp"
        android:textSize="@dimen/_20ssp"
        android:textColor="@color/white" />
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerviewAdmin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:listitem="@layout/item_manger"
        tools:itemCount="3"/>

    <TextView
        android:id="@+id/text_home2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="DoctorWaiting"
        android:background="@drawable/drugs_bar"
        android:layout_margin="@dimen/_20sdp"
        android:padding="@dimen/_5sdp"
        android:textAlignment="center"
        android:textSize="@dimen/_20ssp"
        android:textColor="@color/white"/>
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerview2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:listitem="@layout/item_manger"
        tools:itemCount="3"/>
</LinearLayout>